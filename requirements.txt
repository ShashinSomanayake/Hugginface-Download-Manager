# HuggingFace Download Manager - Requirements
# Install with: pip install -r requirements.txt

# Qt GUI framework (try PySide6 first, PyQt6 as fallback)
PySide6>=6.4.0

# HuggingFace Hub Python SDK
huggingface_hub>=0.20.0

# HTTP requests library
requests>=2.28.0

# Progress bars (used internally by hf_hub)
tqdm>=4.65.0

# Optional: if you want to use PyQt6 instead of PySide6, comment out PySide6 above and use:
# PyQt6>=6.4.0
